{
  "$schema": "https://json-schema.org/draft-07/schema",
  "$id": "https://github.com/z451047442-debug/agency-agents/schemas/agent-frontmatter.schema.json",
  "title": "Agency Agent Frontmatter",
  "description": "Schema for YAML frontmatter in agency-agents .md files. Defines the metadata contract every agent file must satisfy.",
  "type": "object",
  "required": ["name", "description", "emoji", "color"],
  "properties": {
    "name": {
      "type": "string",
      "minLength": 1,
      "maxLength": 120,
      "description": "Display name shown in tool selectors and UI."
    },
    "description": {
      "type": "string",
      "minLength": 10,
      "maxLength": 500,
      "description": "One-sentence summary of the agent's expertise. Used in tool selectors and search."
    },
    "emoji": {
      "type": "string",
      "minLength": 1,
      "maxLength": 8,
      "description": "Single emoji or short emoji sequence representing the agent."
    },
    "color": {
      "type": "string",
      "minLength": 1,
      "maxLength": 32,
      "pattern": "^(#[0-9a-fA-F]{6}|[a-z][a-z-]+[a-z]|cyan|blue|green|red|purple|orange|teal|indigo|pink|gold|amber|yellow|violet|rose|lime|gray|fuchsia|neon-green|neon-cyan|metallic-blue|brown|black|white|silver|maroon|navy|olive|slate)$",
      "description": "Display colour for the agent. Standard named colours: cyan, blue, green, red, purple, orange, teal, indigo, pink, gold, amber, yellow, violet, rose, lime, gray, fuchsia, navy, slate, brown. Hex codes (#RRGGBB) are also accepted.",
      "examples": ["blue", "teal", "#E63946"]
    },
    "vibe": {
      "type": "string",
      "maxLength": 500,
      "description": "Personality primer line that captures the agent's tone and philosophy. Recommended but not required."
    },
    "version": {
      "type": "string",
      "maxLength": 32,
      "description": "Semantic version for the agent definition (e.g. '1.0.0'). Recommended for change tracking.",
      "examples": ["1.0.0"]
    },
    "date_added": {
      "type": "string",
      "maxLength": 16,
      "pattern": "^\\d{4}-\\d{2}-\\d{2}$",
      "description": "ISO date when this agent was first added (e.g. '2026-06-18'). Recommended for lifecycle tracking.",
      "examples": ["2026-06-18"]
    },
    "nexus_roles": {
      "type": "array",
      "description": "NEXUS pipeline phases this agent participates in. Used by the orchestration layer for agent discovery.",
      "items": {
        "type": "string",
        "enum": [
          "phase-0-discovery",
          "phase-1-strategy",
          "phase-2-foundation",
          "phase-3-build",
          "phase-4-hardening",
          "phase-5-launch",
          "phase-6-operate"
        ]
      },
      "uniqueItems": true,
      "minItems": 1
    },
    "lifecycle": {
      "type": "string",
      "enum": ["draft", "review", "published", "deprecated"],
      "description": "Lifecycle stage of this agent definition. Managed by scripts/agent-lifecycle.py."
    },
    "model_tier": {
      "type": "string",
      "enum": ["premium", "standard", "economy"],
      "description": "Cost tier for model routing. premium=Opus (complex/high-stakes), standard=Sonnet (implementation), economy=Haiku (simple/draft)."
    },
    "tdd_framework": {
      "type": "string",
      "maxLength": 120,
      "description": "Test framework for TDD enforcement (e.g. 'pytest --cov', 'go test -cover', 'cargo test'). Injected by NEXUS during build phases."
    },
    "tags": {
      "type": "array",
      "description": "Free-form tags for categorization and discovery.",
      "items": {
        "type": "string",
        "maxLength": 64
      },
      "uniqueItems": true
    },
    "keywords": {
      "type": "array",
      "description": "Structured search keywords for improved discoverability.",
      "items": {
        "type": "string",
        "maxLength": 80
      },
      "uniqueItems": true
    },
    "complexity": {
      "type": "string",
      "enum": ["low", "medium", "high"],
      "description": "Estimated complexity level of the agent's domain or tasks."
    },
    "estimated_duration": {
      "type": "string",
      "maxLength": 32,
      "description": "Expected task duration hint (e.g., '2-4h', '1-2d')."
    },
    "compatible_platforms": {
      "type": "array",
      "description": "AI coding platforms this agent is compatible with.",
      "items": {
        "type": "string",
        "enum": ["claude-code", "cursor", "copilot", "gemini-cli", "codex", "windsurf", "aider", "opencode", "qwen", "kimi", "osaurus", "antigravity", "openclaw", "hermes"]
      },
      "uniqueItems": true
    },
    "depends_on": {
      "type": "array",
      "description": "Agent IDs that this agent logically depends on. The install tool may warn if a dependency is missing.",
      "items": {
        "type": "string",
        "pattern": "^[a-z][a-z0-9]*-[a-z][a-z0-9-]*$"
      },
      "uniqueItems": true,
      "minItems": 1
    },
    "tools": {
      "type": "string",
      "maxLength": 200,
      "description": "Comma-separated tool names this agent is expected to use (e.g. 'Read, Write, Edit, Bash, Grep, Glob')."
    },
    "category": {
      "type": "string",
      "maxLength": 64,
      "description": "Category directory this agent belongs to (informational; derived from the file path)."
    }
  },
  "additionalProperties": false
}
