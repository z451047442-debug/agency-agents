# Git Hooks

## pre-commit

Auto-validates agent files before each commit.

Install:
```
cp scripts/git-hooks/pre-commit .git/hooks/pre-commit && chmod +x .git/hooks/pre-commit
```

Uninstall:
```
rm .git/hooks/pre-commit
```
